version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-next-app
    ports:
      - "3005:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
